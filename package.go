package go_reopen

// ReOpen is package the provides the ReOpener interface and provides a reopenable file.
// It's a common pattern to require closing and re-opening
// log files during rotation. This structure makes this easy with a single call to "ReOpen()"
